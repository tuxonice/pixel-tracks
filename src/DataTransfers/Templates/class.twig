<?php

namespace PixelTrack\DataTransferObjects;

use PixelTrack\DataTransfers\AbstractTransfer;

class {{ className }} extends {{ abstractClass }}
{
{% for property in properties %}
    {{ property.visibility }} {{ property.type }} ${{ property.camelCaseName }};

{% endfor %}


{% for property in properties %}

    public function {{ property.getPrefix }}{{ property.camelCaseName|capitalize }}(): {{ property.type }}
    {
        return $this->{{ property.camelCaseName }};
    }

    public function set{{ property.camelCaseName|capitalize }}({{ property.type }} ${{ property.camelCaseName }}): self
    {
        $this->{{ property.camelCaseName }} = ${{ property.camelCaseName }};

        return $this;
    }
{% if not loop.last %}

{% endif %}
{% endfor %}
}
